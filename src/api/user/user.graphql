scalar Error

type Query {
  me: User
  user(userId: ID!): User
  users: [User!]
}

type User {
  userId: ID!
  email: String!
  password: String!
}

type Mutation {
  register(email: String!, password: String!): Error!
  verify(token: String!): Error!
}
